package edu.kit.pse.a1sicht.ui.shared

import androidx.test.rule.ActivityTestRule
import androidx.test.runner.AndroidJUnit4
import org.junit.Rule
import org.junit.runner.RunWith


/**
 * Example test class for [LogInActivity] class.
 *
 *  TODO
 *  no idea why this isn't working (same for HomeAdminActivity)
 *
@RunWith(AndroidJUnit4::class)
class LoginActivityTest{

    @get:Rule
    var activityScenarioRule = ActivityTestRule(LoginActivity::class.java)

    }
}
 */